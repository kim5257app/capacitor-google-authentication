import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kim5257.capacitor.auth.google',
  appName: 'example',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
